import express from "express";

const router = express.Router();

// Placeholder for WMS routes
router.get("/", (req, res) => {
  res.json({ message: "WMS routes - coming soon" });
});

export default router;
