// router.post('/split-orders/:id/finalize', authenticate, checkPermission('warehouse.split_order.finalize'), async (req,res)=>{});
// router.post('/split-orders/:id/cancel', authenticate, checkPermission('warehouse.split_order.cancel'), async (req,res)=>{});
